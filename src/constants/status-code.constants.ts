export enum HttpStatusCode {
    OK = 200,
    FORBIDDEN = 403,
    NOT_FOUND = 404,
    CONFLICT = 409,
    INTERNAL_SERVER_ERROR = 500,
}

export enum StatusText {
    NOT_FOUND = 'Email не найден',
    EMPTY_MESSAGE = '',
}
